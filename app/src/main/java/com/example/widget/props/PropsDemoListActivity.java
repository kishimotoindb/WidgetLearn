package com.example.widget.props;

import com.example.widget.BaseActivity;
import com.example.widget.edittext.RequestFocusInScrollLayoutActivity;

public class PropsDemoListActivity extends BaseActivity {

  {
    mTitle = new String[]{
        "enable属性对控件颜色的影响",
    };
    mClasses = new Class[]{
        EnableStateColorExprActivity.class,
    };
  }
}